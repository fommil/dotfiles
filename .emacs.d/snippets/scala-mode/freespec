# -*- mode: snippet -*-
#name: freespec
# --
import org.scalatest._
import org.scalatest.Matchers._

class `(file-name-sans-extension (buffer-name))` extends FreeSpec {
  "`(file-name-sans-extension (buffer-name))`" - {
    "$1" in {
      $0
    }
  }
}